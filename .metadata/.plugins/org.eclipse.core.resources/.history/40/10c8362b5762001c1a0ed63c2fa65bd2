package com.hw2.run;

import java.util.Scanner;

import com.hw2.model.vo.Student;

public class Run {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner sc = new Scanner(System.in);
		Student[] sArr = new Student[10];
		
		int count = 0;

		while(true) {
			int grade = sc.nextInt();
			sArr[count].setGrade(grade);
			
			int classroom = sc.nextInt();
			sArr[count].setClassroom(classroom);
			
			sc.nextLine();
			
			String name = sc.nextLine();
			sArr[count].setName(name);
			
			int kor = sc.nextInt();
			sArr[count].setKor(kor);
			
			int eng = sc.nextInt();
			sArr[count].setEng(eng);
			
			int math = sc.nextInt();
			sArr[count].setMath(math);
			
			sc.nextLine();
			
			count++;
			
			System.out.println("계속 추가하시겠습니까?(y/n)");
			char chk = sc.nextLine().charAt(0);
			
			if(chk == 'n') {
				return;
			}
		}
	}

}
